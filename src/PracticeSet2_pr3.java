public class PracticeSet2_pr3 {
    public static void main(strings[] args) {
        // Initialize variables
        int v = 10; // replace 10 with the actual value
        int u = 5;  // replace 5 with the actual value
        int a = 2;  // replace 2 with the actual value
        int s = 8;  // replace 8 with the actual value

        // Calculate x using the formula
        int x = (v * v - u * u) / (2 * a * s);

        // Print the result
        System.out.println("The value of x is: " + x);
    }
}
